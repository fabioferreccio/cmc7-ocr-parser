/**
 * Vue 3 composable for CMC-7 reader.
 * Exported via subpath export `cmc7-ocr-parser/vue` (docs/03-arquitetura.md §5.3).
 *
 * Implemented in T-024.
 */

// TODO (T-024): Implement useCMC7Reader composable

export function useCMC7Reader(): never {
  throw new Error('Vue composable not yet implemented. See docs/04-tasks.md T-024.');
}
