/**
 * Detects and crops the CMC-7 strip from a binarized check image.
 * Uses horizontal projection profile to locate the high-density pixel region.
 *
 * @remarks Camada 1 — Image Pipeline (docs/03-arquitetura.md §1.2, step 8)
 * Implemented in T-011.
 */
export class ROIDetector {
  // TODO (T-011): Implement horizontal projection + sliding window detection
}
