/**
 * Parses the raw CMC-7 string into structured fields.
 *
 * @remarks Camada 3 — Parsing (docs/03-arquitetura.md §3.1)
 * Implements the CMC-7 Brazilian grammar with bank-specific layouts via BankRegistry.
 * Implemented in T-017.
 */
export class CMC7Parser {
  // TODO (T-017): Implement symbol splitting + bank-aware field extraction
}
